# Firebase scaffolding

This folder contains Firebase Auth + Firestore scaffolding.

Dependencies intentionally NOT added yet (per scaffolding-only request).

Later install:
- `firebase` (client)
- `firebase-admin` (server)

Then replace placeholder implementations in this folder.
