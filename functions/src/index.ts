// Firebase Cloud Functions scaffold.
//
// Later you will:
// - Install firebase-functions + firebase-admin + stripe (and optionally mux-node)
// - Implement webhook + API endpoints that mirror src/pages/api
//
// This file intentionally has no runtime imports so the repo builds without new deps.

export {}
