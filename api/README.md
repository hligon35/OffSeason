# API (architecture placeholder)

The functional API handlers live in `src/api/*` and are wired through Next.js API routes in `src/pages/api/*`.

This folder exists to match the requested architecture paths without changing the build structure.
